---
description: Import latest Antigravity memory from cloud storage to local machine
---

// turbo-all

1. Run the sync script in import mode (cloud → local)

```powershell
& "$env:USERPROFILE\.gemini\antigravity\skills\memory-sync\scripts\sync.ps1" -Direction import
```

1. Report what was imported to the user
