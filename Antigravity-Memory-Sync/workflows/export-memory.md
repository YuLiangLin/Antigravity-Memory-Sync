---
description: Export local Antigravity memory to cloud storage for other machines to access
---

// turbo-all

1. Run the sync script in export mode (local → cloud)

```powershell
& "$env:USERPROFILE\.gemini\antigravity\skills\memory-sync\scripts\sync.ps1" -Direction export
```

1. Report what was exported to the user
