---
description: Sync Antigravity memory (brain + knowledge + skills) bidirectionally with cloud storage
---

// turbo-all

1. Run the sync script in bidirectional mode

```powershell
& "$env:USERPROFILE\.gemini\antigravity\skills\memory-sync\scripts\sync.ps1"
```

1. Report the sync results to the user
